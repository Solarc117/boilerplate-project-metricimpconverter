const chaiHttp = require('chai-http'),
  chai = require('chai'),
  { assert } = chai,
  server = require('../server.js'),
  CONVERT_PATH = '/api/convert'

chai.use(chaiHttp)

suite('Issue Tracker', () => {
  test('1. ')
})
